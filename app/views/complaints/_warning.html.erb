<div class="overlay hidden">
  <div class="main-content">
    <h3><span>Estás por dirigirte al formulario de consulta</span><i class="fa-solid fa-xmark"></i></h3>
    <p>Puedes ingresar una nueva consulta o modificar la consulta que has realizado, por favor indica la opción correcta</p>
    <div class="actions">
      <% if params[:token] %>
        <a href="/nueva-apelacion?token=<%= params[:token] %>" class="btn mainButton back-confirm">Realizar cambios en la consulta</a>
      <% end %>
      <a href="/nueva-apelacion" class="btn mainButton back-confirm">Realizar una nueva consulta</a>
      <a href="#" class="back-cancel"><h3>Volver al paso 3</h3></a>
    </div>
  </div>
</div>
<script>
  $('.back-confirm').click(() => {
    slideTo(2, 3);
  });
  $('.back-cancel').click((e) => {
    e.preventDefault();
    $('.overlay').addClass('hidden');
  });
  $('.fa-xmark').click(() => {
    $('.overlay').addClass('hidden');
  });
</script>
